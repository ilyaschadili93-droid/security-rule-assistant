import { FilterRulePipe } from './filter-rule-pipe';

describe('FilterRulePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRulePipe();
    expect(pipe).toBeTruthy();
  });
});
